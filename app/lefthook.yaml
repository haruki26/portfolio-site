pre-commit:
  commands:
    check:
      root: app/
      glob: "src/**/*.{ts,tsx}"
      run: biome check --write {staged_files}

pre-push:
  parallel: true
  commands:
    type-check:
      root: app/
      run: "pnpm ci:type"

    test:
      root: app/
      run: pnpm test

