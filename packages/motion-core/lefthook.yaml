pre-commit:
  parallel: true
  commands:

    check-ts:
      root: packages/motion-core/
      glob: "ts/**/*.ts"
      run: biome check --write {staged_files}

    check-rs:
      root: packages/motion-core/
      glob: "src/**/*.rs"
      run: rustfmt {staged_files} && cargo clippy

